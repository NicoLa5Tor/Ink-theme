/** Scroll de la cortina / fold / copy (el logo ya no consume scroll). */
export const HERO_REVEAL_VH = 1.5;

/** Distancia de pin antes de que la página baje de verdad. */
export const HERO_PIN_VH = HERO_REVEAL_VH;
